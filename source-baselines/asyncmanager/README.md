# AsyncManager Rate Limit source excerpt

- Source repository identity: AsyncManager (the supplied ZIP's Git metadata records branch `production`).
- Source commit: `82a2887c5a67a640145b4428e50a292b7a425b93`.
- Commit date: 2026-07-27 15:54:56 +03:30.
- Scope: 18 selected Java files needed to trace the Rate Limit Manager routes, validation, persistence call, node selection and response aggregation.
- This is a curated excerpt, not a complete repository snapshot. It cannot establish repository-wide deletions, buildability or runtime configuration.
- Files outside Java source, IDE metadata, build output, binaries, configuration and credentials were excluded.
- SHA-256 values are recorded in `source-manifest.sha256`.
- The original uploaded DOCX is not copied here; its structured observations are recorded in `evidence/rate-limit-guide-observations.md`.
